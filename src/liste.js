document.addEventListener('DOMContentLoaded', () => {
    const listeContainer = document.getElementById('liste');
    // À compléter avec la logique pour gérer la liste de courses
    listeContainer.textContent = 'Votre liste de courses sera affichée ici.';
  });
  